[package]
name = "libchibi"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2.164"

[build-dependencies]
cc = "1.0"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[build]
opt-level = 3            
lto = true
rustflags = ["-C", "target-f=+crt-static"]

